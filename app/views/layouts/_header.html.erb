<header class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <%= link_to "runnery", root_path, id: "logo" %>
          <nav>
            <ul class="nav">
              <% if signed_in? %>
            <li><%= link_to "Users", users_path %></li>
          <% end %>
            </ul>
          </nav>
        </div>
      </div>
    </header>